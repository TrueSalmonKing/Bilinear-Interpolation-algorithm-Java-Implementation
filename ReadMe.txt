Place both the software and the image to be processed in the folder for ease of use.For better results, the algorithm is run multiple times when the upscaling or downscaling ratio is twice the original dimension.T/N: Height and Width ratios to be inputed are the ratios that will be used to scale, and NOT the dimensions of the image to be created.						////// SalmonKing ///////